services:
  - type: web
    name: keycloak
    env: docker
    plan: free
    region: oregon
    autoDeploy: false
    dockerfilePath: ./Dockerfile
    envVars:
      - key: KEYCLOAK_ADMIN
        value: admin
      - key: KEYCLOAK_ADMIN_PASSWORD
        value: admin